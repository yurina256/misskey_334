{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "スクリプトの実行: test",
            "request": "launch",
            "command": "node index.js",
            "cwd": "${workspaceFolder}"
        }
    ]
}